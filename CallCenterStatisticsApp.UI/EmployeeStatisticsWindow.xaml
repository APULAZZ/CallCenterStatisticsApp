<Window x:Class="CallCenterStatisticsApp.UI.EmployeeStatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Статистика по сотрудникам"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Статистика по сотрудникам"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

		<StackPanel Grid.Row="1">
			<WrapPanel Margin="0,0,0,10">
				<StackPanel Margin="0,0,15,0">
					<TextBlock Text="Дата с"/>
					<DatePicker x:Name="FromDatePicker" Width="150"/>
				</StackPanel>

				<StackPanel Margin="0,0,15,0">
					<TextBlock Text="Дата по"/>
					<DatePicker x:Name="ToDatePicker" Width="150"/>
				</StackPanel>

				<StackPanel VerticalAlignment="Bottom">
					<Button Content="Обновить"
                            Width="120"
                            Height="34"
                            Margin="0,20,0,0"
                            Click="RefreshButton_Click"/>
				</StackPanel>
			</WrapPanel>

			<TextBlock x:Name="SummaryTextBlock"
                       Margin="0,0,0,10"
                       FontWeight="SemiBold"
                       Foreground="DarkBlue"
                       Text="Записей: 0"/>
		</StackPanel>

		<DataGrid x:Name="StatsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  GridLinesVisibility="All"
                  RowHeight="26"
                  ColumnHeaderHeight="32">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Сотрудник"
                                    Binding="{Binding EmployeeName}"
                                    Width="*"/>

				<DataGridTextColumn Header="Входящие"
                                    Binding="{Binding IncomingCount}"
                                    Width="110"/>

				<DataGridTextColumn Header="Пропущенные входящие"
                                    Binding="{Binding IncomingMissedCount}"
                                    Width="170"/>

				<DataGridTextColumn Header="Исходящие"
                                    Binding="{Binding OutgoingCount}"
                                    Width="110"/>

				<DataGridTextColumn Header="Исходящие без ответа"
                                    Binding="{Binding OutgoingNoAnswerCount}"
                                    Width="170"/>
			</DataGrid.Columns>
		</DataGrid>

		<StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
			<Button Content="Назад"
                    Width="110"
                    Height="34"
                    Click="BackButton_Click"/>
		</StackPanel>
	</Grid>
</Window>